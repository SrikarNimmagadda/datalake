# configuration parameters

# all this should be set up as environment variables

aws_access_key_id = "redacted"  # shouldn't need this
aws_secret_access_key = "redacted"  # shouldn't need this
region_name = "us-east-1"
dynamodb_table = "tb-us-east-1-dev-metadata"
